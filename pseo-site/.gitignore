# Dependencies & Build Outputs
node_modules
.next
out
# scripts/split-by-keyword.mjs가 out/를 키워드별로 나눈 임시 배포용 폴더들(out-도배업체 등).
# Cloudflare Pages 무료 플랜 2만 파일 제한 때문에 나눈 것 — lib/constants.ts 참고.
out-*

# scripts/generate-sitemap.mjs가 빌드할 때마다 새로 만든다(DB 상태 그대로 반영) — 직접 편집 X
public/sitemap.xml
public/robots.txt

# Environment Variables & Secrets
.env
.env.local
.env.production
.env.development

# Log files & OS metadata
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Temporary & Cache files
.cache/
.vercel
tsconfig.tsbuildinfo
