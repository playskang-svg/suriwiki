# react@19.0.0-rc-... 는 Next 15.0.0 이 고정한 프리릴리스다.
# semver 에서 프리릴리스는 lucide-react 가 요구하는 ^19.0.0 범위에 포함되지 않아
# 새로 설치하는 환경(Vercel 등)에서 npm install 이 ERESOLVE 로 죽는다.
#
# 실제로는 호환된다 — 이 조합으로 next build 가 통과한다.
# Next 를 정식 react 19 로 올리면 이 줄을 지워라.
legacy-peer-deps=true
